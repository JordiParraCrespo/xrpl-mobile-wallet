import { ScreenStub } from '../../components/drops/screen-stub';

export default function ReceiveScreen() {
  return (
    <ScreenStub
      eyebrow="Receive"
      title="Get paid"
      blurb="An account switcher (XRP Ledger / XRPL EVM), a QR with the Dewy mark, tap-to-copy address, destination tag and a network safety warning."
      design="receive.html · flows/receive-app.jsx (+ flow-kit.jsx)"
    />
  );
}
