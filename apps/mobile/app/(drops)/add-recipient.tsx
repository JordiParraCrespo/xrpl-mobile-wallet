import { ScreenStub } from "../../components/drops/screen-stub";

export default function AddRecipientScreen() {
  return (
    <ScreenStub
      eyebrow="Payments"
      title="Add recipient"
      blurb="Individual or business, network, address (with paste + QR scan), an optional destination tag and a name. XRPL transactions can't be reversed once sent."
      design="payments/payments-screens.jsx (Add recipient)"
    />
  );
}
