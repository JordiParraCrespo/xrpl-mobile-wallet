import { ScreenStub } from "../../../components/drops/screen-stub";

export default function AddMoneyScreen() {
  return (
    <ScreenStub
      eyebrow="To XRP Ledger · $744.87"
      title="Add money"
      blurb="A large local-currency amount with a no-fee line, the funding source, an 'usually instantly' note, and the shared keypad."
      design="addMoney.html · flows/addmoney-app.jsx (+ flow-kit.jsx)"
    />
  );
}
