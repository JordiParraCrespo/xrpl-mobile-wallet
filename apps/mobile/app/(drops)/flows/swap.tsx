import { ScreenStub } from "../../../components/drops/screen-stub";

export default function SwapScreen() {
  return (
    <ScreenStub
      eyebrow="Swap"
      title="Convert tokens"
      blurb="From / To token cards with a flip control, the rate and network fee rows, a balance-guarded Review swap CTA, and the shared keypad."
      design="swap.html · flows/swap-app.jsx (+ flow-kit.jsx)"
    />
  );
}
