import { ScreenStub } from '../components/drops/screen-stub';

export default function ProfileScreen() {
  return (
    <ScreenStub
      eyebrow="Profile & settings"
      title="Jordan"
      blurb="Glass-on-gradient settings: Account (wallets · recovery phrase & security), Preferences (currency · appearance), Support, and a red Danger zone (lock · sign out)."
      design="profile.html · profile/profile-app.jsx"
    />
  );
}
